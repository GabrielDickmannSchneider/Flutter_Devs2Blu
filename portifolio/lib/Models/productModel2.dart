class ProductModel2 {
  String title;
  String description;
  String manufactor;
  double cost;
  String imageURL;

  ProductModel2(
      {required this.title,
        required this.description,
        required this.manufactor,
        required this.cost,
        required this.imageURL});
}
